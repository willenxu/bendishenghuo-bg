# 秒送 Tab 二次搜索闭环 — 界面规范（MD）

> **设计稿来源**：[Relay 设计稿](https://relay.jd.com/file/design?id=2056991638009413633&page_id=0%3A2)  
> **短链**：https://3.cn/-11gP2mrO  
> **画布名称**：秒送tab二次搜索闭环  
> **设备规格**：375 × 812（iPhone 标准屏）  
> **页面数量**：2（浅色 / 暗黑）

---

## 1. 产品背景

用户在**搜索结果页**的 **「秒送」Tab** 内发起二次搜索（带搜索词标签），当秒送域内**无匹配商品**时，展示半屏缺省态，并提供「搜索全站」兜底能力，形成二次搜索闭环。

| 场景 | 说明 |
|------|------|
| 入口 | 搜索页 → Tab「秒送」选中 |
| 触发条件 | 当前关键词在秒送 Tab 下无商品结果 |
| 核心目标 | 告知无结果 + 引导全站搜索 |

---

## 2. 页面清单

| 序号 | 页面名称 | 节点 ID | 主题 | 说明 |
|------|----------|---------|------|------|
| P1 | 秒送 Tab 无结果（浅色） | `6:37` | Light | 主稿，完整筛选区 + 缺省态 |
| P2 | 秒送 Tab 无结果（暗黑） | `13:1` | Dark | 与 P1 结构一致，色值映射 |

---

## 3. 信息架构（自上而下）

```text
┌─────────────────────────────────────┐
│ 系统状态栏 (44px)                    │
├─────────────────────────────────────┤
│ 导航栏：返回 + 搜索框 + 竖卡切换 (44px) │
├─────────────────────────────────────┤
│ 一级 Tab：全部/新品/秒送/店铺/口碑 (39px) │
├─────────────────────────────────────┤
│ 筛选工具栏 Row1 (30px)               │
│  - 地址 | 综合排序 | 切到门店 | 筛选   │
├─────────────────────────────────────┤
│ 快捷筛选 Chips 横向滚动 (≈30.5px)     │
├─────────────────────────────────────┤
│ 内容区渐变背景 (291px 起)             │
│   └─ 半屏缺省态（居中）               │
├─────────────────────────────────────┤
│ Home Indicator (34px)                │
└─────────────────────────────────────┘
```

**垂直锚点（浅色稿）**

| 区块 | Y 起点 | 高度 |
|------|--------|------|
| 顶栏容器 `6:575` | 0 | 127 |
| 筛选区 `6:39` | 136 | 64.5 |
| 内容渐变 `6:38` | 128 | 291 |
| 缺省态 `6:729` | 261 | 180 |
| Home Indicator `6:573` | 778 | 34 |

---

## 4. 模块规范

### 4.1 系统状态栏

| 属性 | 浅色 | 暗黑 |
|------|------|------|
| 高度 | 44px | 44px |
| 时间字号 | 15px / Semibold | 15px / Semibold |
| 时间颜色 | `#1A1A1A` | `#E6E6E6` |
| 组件 | `Navigation / 状态栏 / 黑字` | `Navigation / 状态栏 / 白字` |

---

### 4.2 导航搜索栏

**布局**：左右 padding 16px，上下 padding 6px，内容区高度 32px。

| 元素 | 尺寸 | 行为 | 节点（浅/深） |
|------|------|------|---------------|
| 返回 | 20×20 | 返回上一页 | `6:597` / `13:237` |
| 搜索框 | flex 1，圆角 8px | 展示已选搜索词标签 | `6:599` / `13:241` |
| 竖卡切换 | 20×20 | 切换列表/卡片视图 | `6:609` / `13:239` |

**搜索框样式**

| 属性 | 浅色 | 暗黑 |
|------|------|------|
| 背景 | `#FFFFFF` | `#1F1F1F` |
| 描边 | 无 | `0.5px #FF5C67` |
| 阴影 | `0 0 4px rgba(0,0,0,0.05)` | 无 |

**搜索热词标签（组件：搜索热词）**

| 属性 | 浅色 | 暗黑 |
|------|------|------|
| 内边距 | 8×4 | 8×4 |
| 圆角 | 4px | 4px |
| 背景 | `#F4F4F4` | `#0A0A0A` |
| 文案 | 12px Regular | 12px Regular |
| 文案色 | `#1A1A1A` | `#E6E6E6` |
| 关闭图标 | 6×6 `icon/关闭/6/灰` | 同左 |
| 示例文案 | 金枪鱼饼干 | SPA |

**右侧能力**

| 图标 | 尺寸 | 说明 |
|------|------|------|
| 相机 | 20×20 | 图搜入口 |

---

### 4.3 一级 Tab 导航

| 属性 | 值 |
|------|-----|
| 容器高度 | 39px |
| 左右 padding | 16px |
| Tab 间距 | 44px |
| Tab 项 | 全部、新品、**秒送**、店铺、口碑 |

**Tab 文字样式**

| 状态 | 字号 | 字重 | 颜色 | 字体 |
|------|------|------|------|------|
| 默认 | 16px | Regular | `#1A1A1A`（浅）/ `#E6E6E6`（深） | PingFang SC |
| 选中（秒送） | 19px | Semibold | `#FF0400` | 京东朗正体 JingDongLangZhengTi |

> 本稿固定为「秒送」选中态。

---

### 4.4 筛选工具栏（Row 1）

| 属性 | 值 |
|------|-----|
| 高度 | 30px（含 bottom padding 8px） |
| 左右 padding | 12px |
| 布局 | space-between，单行 |

| 区块 | 文案示例 | 样式 | 交互 |
|------|----------|------|------|
| 地址 | 龙岗区爱路... | 14px，`#3D414D`（浅）/ `#999999`（深） | 点击展开地址选择 |
| 综合排序 | 综合排序 | 14px Semibold，`#171A26`（浅）/ `#E6E6E6`（深） | 下拉排序 |
| 切到门店 | 切到门店 | 14px Regular，次要色 | 切换门店模式 |
| 分隔线 | 1×20 渐变竖线 | 视觉分隔 | — |
| 筛选 | 筛选 + 漏斗图标 | 14px Regular | 打开筛选面板 |

---

### 4.5 快捷筛选 Chips（横向滚动）

| 属性 | 值 |
|------|-----|
| 容器 | 宽 363，左 padding 12，gap 8，横向溢出滚动 |
| Chip 高度 | 24px（padding 6×10/12） |
| 圆角 | 4px |

**Chip 样式**

| 类型 | 背景（浅/深） | 文字色 | 附加 |
|------|---------------|--------|------|
| 默认 | `#F2F3F7` / `#0A0A0A` | `#1A1A1A` 或 `#171A26` / `#E6E6E6` | — |
| 强调（促销） | 同上 | `#FF0000` | 如「减免运费」 |
| 带下拉 | 同上 | 同上 | 右侧 6×6 展开箭头 |

**示例 Chips（设计稿顺序）**

1. 30分钟达到  
2. 减免运费（红色强调）  
3. 领满减券  
4. 包装形式（带下拉）  
5. 产地（带下拉）  
6. 品牌 Logo 图（60.16×12.5，图片 Chip）

---

### 4.6 内容区背景

| 属性 | 浅色 | 暗黑 |
|------|------|------|
| 区域 | y=128，高 291 | y=284，高 120（渐变层 `13:287`） |
| 渐变 | `rgba(255,255,255,0.98)` → `rgba(245,246,250,0.98)` | `#141414` → `#1F1F1F` |
| 页面底 | `#F5F6FA` | `#141414` |

---

### 4.7 半屏缺省态（核心）

**组件实例**：`缺省状态/半屏缺省`（`6:729` / `13:366`）

| 属性 | 值 |
|------|-----|
| 容器宽 | 240px |
| 水平居中 | left = (375-240)/2 = 64 |
| 垂直位置 | top = 261 |
| 内部 gap | 16px |

**结构**

```text
[插画 120×120]
[描述文案]
[操作按钮区]
```

| 子元素 | 规格 | 内容 |
|--------|------|------|
| 插画 | 120×120，`搜索为空` | Joy 吉祥物 + 放大镜（浅色 100% / 暗黑 60% 透明度） |
| 描述 | 12px Regular，居中 | **未找到相关商品** |
| 描述色 | `#3D414D`（浅）/ `#999999`（深） | — |
| 主按钮 | 高 32，min-width 60，padding 12×9，圆角 4 | **搜索全站** |
| 按钮背景 | `#FF0F23` | 深浅一致 |
| 按钮文字 | 12px Medium，`#FFFFFF` | — |

> 设计稿中**仅展示主按钮**「搜索全站」；组件库支持负向按钮（白底），本场景未启用。

---

### 4.8 Home Indicator

| 属性 | 浅色 | 暗黑 |
|------|------|------|
| 区域高 | 34px | 34px |
| 条尺寸 | 134×5，圆角 3px | 同左 |
| 颜色 | `#171A26` | `#E6E6E6` |
| 位置 | 水平居中，top 21 | 同左 |

---

## 5. 设计 Token

### 5.1 色彩

| Token | 浅色 | 暗黑 | 用途 |
|-------|------|------|------|
| `page-bg` | `#F5F6FA` | `#141414` | 页面背景 |
| `text-primary` | `#1A1A1A` / `#171A26` | `#E6E6E6` | 主文案 |
| `text-secondary` | `#3D414D` | `#999999` | 次要文案 |
| `text-tab-active` | `#FF0400` | `#FF0400` | 选中 Tab |
| `brand-red` | `#FF0F23` | `#FF0F23` | 主按钮 |
| `promo-red` | `#FF0000` | `#FF0000` | 促销 Chip |
| `chip-bg` | `#F2F3F7` | `#0A0A0A` | 筛选 Chip |
| `search-tag-bg` | `#F4F4F4` | `#0A0A0A` | 搜索词标签 |
| `search-box-bg` | `#FFFFFF` | `#1F1F1F` | 搜索框 |
| `search-box-border` | — | `#FF5C67` 0.5px | 暗黑搜索框描边 |

### 5.2 字体

| Token | 值 |
|-------|-----|
| 主字体 | PingFang SC |
| 品牌 Tab 字体 | 京东朗正体（JingDongLangZhengTi） |
| 12 / 14 / 15 / 16 / 19 | 对应 line-height = font-size（单行） |

### 5.3 圆角 & 间距

| Token | 值 |
|-------|-----|
| `radius-sm` | 4px（Chip、按钮、标签） |
| `radius-md` | 8px（搜索框） |
| `spacing-page` | 16px |
| `spacing-filter` | 12px |
| `gap-tab` | 44px |
| `gap-chip` | 8px |

---

## 6. 交互说明

| 操作 | 行为 |
|------|------|
| 点击返回 | 退出当前搜索页 |
| 点击搜索词 × | 清除当前二次搜索词 |
| 点击相机 | 进入拍照/图搜 |
| 点击竖卡切换 | 切换商品展示形态 |
| 切换 Tab（非秒送） | 离开本闭环页面 |
| 点击地址/排序/筛选 | 打开对应面板（本稿为静态展示） |
| 横向滑动 Chips | 浏览更多筛选项 |
| 点击「搜索全站」 | 以当前关键词跳转全站搜索结果 |

---

## 7. 浅色 ↔ 暗黑对照

| 模块 | 浅色节点 | 暗黑节点 | 主要差异 |
|------|----------|----------|----------|
| 整页 | `6:37` | `13:1` | 背景、文字反色 |
| 顶栏 | `6:575` | `13:216` | 状态栏黑字→白字 |
| Tab | `6:611` | `13:409` | 文案 `#E6E6E6` |
| 筛选 | `6:39` | `13:415` | Chip 深色底 |
| 缺省 | `6:729` | `13:366` | 插画 60% 透明度 |
| 搜索框 | 白底无描边 | 深底 + 红色描边 | 强调输入态 |

---

## 8. 组件依赖（设计系统）

| 组件名 | 类型 | 使用位置 |
|--------|------|----------|
| Navigation / 状态栏 | 系统 | 顶部 |
| 搜索框 + 搜索热词 | 业务 | 导航栏 |
| icon/关闭/6/灰 | 图标 | 热词标签 |
| 竖卡切换 | 图标按钮 | 导航右侧 |
| 按钮/28 | 筛选 Chip | 快捷筛选行 |
| 缺省状态/半屏缺省 | 业务 | 内容区 |
| 搜索为空 | 插画 | 缺省态 |
| 样式=主要操作（填充） | 按钮 | CTA |

---

## 9. 开发验收要点

- [ ] 仅在 **秒送 Tab 选中** 且无结果时展示该缺省态  
- [ ] 搜索词以 **标签形式** 固化在搜索框内，可删除  
- [ ] 缺省插画 + 文案 + 单 CTA 垂直居中于内容可视区  
- [ ] 「搜索全站」按钮色值 `#FF0F23`，字号 12px Medium  
- [ ] 筛选区（地址/排序/Chips）在无结果时**仍保留**，不可隐藏  
- [ ] 支持暗黑模式完整色值映射  
- [ ] 适配 375 宽，Home Indicator 安全区 34px  

---

## 10. Relay 节点速查

```
画布 0:2「秒送tab二次搜索闭环」
├── 6:37  浅色主屏
│   ├── 6:575  顶栏（状态栏+搜索+Tab）
│   ├── 6:39   筛选区
│   ├── 6:38   内容渐变底
│   └── 6:729  半屏缺省
└── 13:1  暗黑样式（结构镜像）
```
