---
name: ui-qa-redline-checker
description: 对比设计稿截图与开发实现截图，结构化逐层检查UI还原度问题，输出带精准红框标注的QA表格HTML报告。当用户上传两张截图（设计稿 + 开发实现）并提到"QA"、"还原度"、"走查"、"对比截图"、"找问题"、"开发没做对"、"给开发的反馈"等场景时，必须使用此 skill。即使用户只说"帮我看看这两张图有什么差异"，只要有设计稿和实现图，也应触发此 skill。
---

# UI QA 还原度检查 Skill

## 定位
输入：设计稿截图 + 开发实现截图
输出：结构化 HTML 报告（带精准红框 + 模块分组 + 问题描述 + 自检报告）

---

## ⚠️ 核心铁律

### 铁律1：默认无问题，举证才算问题
- 像素 diff ≠ UI 问题
- 拿不准标"待确认"，绝不硬写

### 铁律2：先看图，再写问题，不靠 diff 反推

### 铁律3：每个模块独立判断（严禁"同上"）

### 铁律4：红框只圈具体错误元素

### 铁律5：必列"已确认无问题区域"

### 铁律6：线性元素必须做轴线扫描

### 铁律7：对齐必须做"多层级"检查（元素↔容器 + 元素↔卡片 + 容器↔卡片）

### 🆕 铁律8：截图来源强制约束
**对比图必须左 = 设计稿、右 = 开发稿，绝不能自我比对**

代码层面强制：
```python
# ❌ 错误示例：两张都从同一张图裁
d_crop = design.crop(...)
v_crop = design.crop(...)  # 错！应该用 dev

# ✅ 正确示例
d_crop = design.crop(...)  # 来源：设计稿
v_crop = dev.crop(...)     # 来源：开发稿
```

**自检方法**：
- 函数命名严格区分 `crop_from_design()` / `crop_from_dev()`
- 红框只画在 dev 图上，design 图保持原样
- 输出报告后，验证 design_b64 ≠ dev_b64（如果相同 → 一定错了）

### 🆕 铁律9：报告输出后必须做"终审自检"
报告生成完成后，**不能直接给用户**，必须先做一轮终审：

```
对每条问题逐一过：
1. 截图来源核对：左边是不是 design？右边是不是 dev？
2. 截图内容与问题描述是否对应？（问题说的是按钮，截图是不是按钮区域？）
3. 红框位置是否精确指向描述的问题元素？
4. "设计是X / 开发是Y" 这两句话是否真的成立？
5. 优先级判定是否合理？
```

任何一条不通过 → 修正后重新生成，再次终审，直到全部通过才能交付。

---

## 执行流程

### Step 1：图像规范化
```python
design = Image.open(DESIGN_PATH).convert("RGB")  # 明确来源
dev    = Image.open(DEV_PATH).convert("RGB")     # 明确来源
if design.size != dev.size:
    dev = dev.resize(design.size, Image.LANCZOS)
```

### Step 2：结构化分块（页面 → 模块 → 组件 → 元素 → 属性）

### Step 3：逐组件肉眼对比 + 9 维度清单

| 维度 | 检查要点 |
|---|---|
| 结构 | 模块齐全、顺序正确 |
| 布局 | 元素 vs 父容器 / 卡片 / 屏幕 三层对齐 |
| 字号 | 各级字号 |
| 字重 | Regular/Medium/Bold |
| 颜色 | 文字/背景/边框/icon 色 |
| 间距 | 元素间距、内外边距 |
| 圆角 | 卡片、按钮、图片 |
| 阴影 | 有无、参数 |
| 图标 | 尺寸、色彩、形状 |
| 尺寸 | 组件宽高 |

### Step 3.5：算法辅助检查（强制）

#### A. 线性元素轴线扫描
```python
def scan_horizontal_axis(design, dev, y_start, y_end, threshold=30, min_gap=20):
    """注意：必须传两张图！design 和 dev"""
    import numpy as np
    W = design.size[0]
    d_arr = np.array(design.crop((0, y_start, W, y_end)).convert("L"))
    v_arr = np.array(dev.crop((0, y_start, W, y_end)).convert("L"))
    # ... 找峰值并比较
```

#### B. 多层对齐检查
```python
def check_alignment_multi_level(design, dev, element_y, container_box, card_box):
    """检查元素在容器中、容器在卡片中、元素在卡片中的三层对齐"""
    # 同时从 design 和 dev 计算，对比偏移量
```

### Step 4：每条候选问题必须能回答
1. 设计稿是什么样？
2. 开发是什么样？
3. 差异是什么？
4. 红框圈哪里？

答不上来 → 删除

### Step 5：生成报告

#### 5.1 裁切代码模版（强制）
```python
def make_compare_crop(design_img, dev_img, crop_box, mark_box, pad=8):
    """
    生成对比图：左设计稿、右开发稿（带红框）
    
    严格约束：
    - design_img 只来自设计稿
    - dev_img 只来自开发稿
    - 红框只画在 dev 上
    """
    cy1, cy2, cx1, cx2 = crop_box  # 裁切范围
    
    # 左图：纯设计稿，无任何标记
    d_crop = design_img.crop((cx1, cy1, cx2, cy2))
    
    # 右图：开发稿 + 红框
    v_crop = dev_img.crop((cx1, cy1, cx2, cy2))
    v_marked = v_crop.copy()
    draw = ImageDraw.Draw(v_marked)
    mx1, my1, mx2, my2 = mark_box  # 相对裁切的坐标
    draw.rectangle([mx1, my1, mx2, my2], outline=(255,30,30), width=3)
    
    return {
        "design": img_to_b64(d_crop),    # ← 必须是 design_img 裁出来的
        "dev":    img_to_b64(v_marked),  # ← 必须是 dev_img 裁出来的
    }
```

#### 5.2 内部验证（自动）
```python
# 每条问题生成后，立即校验
for issue in issues:
    assert issue["design_b64"] != issue["dev_b64"], \
        f"问题 {issue['id']} 的对比图两边相同！必定是用了同一张图！"
```

### 🆕 Step 6：终审自检（强制）

**生成报告 HTML 后，绝不直接返回给用户**。先做终审：

#### 6.1 视觉终审（必须）
读取生成的报告，对每条问题逐一肉眼验证：

```
对 issue #N：
□ 左图（绿色 ✓ 设计稿标签）的内容，看起来像不像设计稿？
□ 右图（红色 ✗ 开发稿标签）的内容，看起来像不像开发稿？
□ 两张图是否明显不同？（如果一模一样，说明用了同一来源 → 错）
□ 红框位置是否对应问题描述？（描述说"按钮宽度"，红框是否在按钮上？）
□ 问题描述的"设计是X、开发是Y"，X和Y是否真的能从图中看出？
```

任何一条不通过 → 标记为缺陷，重新生成。

#### 6.2 数据终审（自动）
```python
def final_review(issues):
    errors = []
    for iss in issues:
        # 检查1：两张图不能相同
        if iss["d_b64"] == iss["v_b64"]:
            errors.append(f"{iss['id']}: 对比图两边相同")
        # 检查2：必须有红框（dev 图被修改过）
        if not iss.get("marked"):
            errors.append(f"{iss['id']}: 开发图未标注红框")
        # 检查3：必填字段
        for f in ["design_val", "dev_val", "desc", "location"]:
            if not iss.get(f):
                errors.append(f"{iss['id']}: 缺失字段 {f}")
    return errors
```

终审报告必须附在最终输出末尾，列出：
- 总问题数 / 通过项 / 待复核项
- 每条问题的终审结论

---

## 输出 HTML 报告结构

```
报告头部
├── 标题 + 元信息
└── 统计卡片

正文表格（按模块分组）
└── 每行：[编号][层级][位置/类型][截图对比][说明][优先级]

🆕 终审自检报告
├── 数据校验结果（自动）
└── 视觉校验结果（逐条）

底部
└── ✅ 已确认无问题区域清单
```

---

## 优先级判断规则
- **高**：品牌感知、视觉层级坍塌、主色错误、布局错乱、对齐偏移 >10px
- **中**：间距偏差明显（>4px）、字号差一级、对齐偏移 3-10px
- **低**：细微偏差（≤2px）

---

## 🔥 失败模式自检清单（输出前必走）

任何一项"否"必须重做：

**A 组：内容正确性**
- [ ] 肉眼看了每个问题截图，没靠 diff 反推？
- [ ] 每条问题能用"设计是X/开发是Y"清晰表达？
- [ ] 每个模块独立判断，没复制结论？
- [ ] 拿不准的标了"待确认"？

**B 组：检查覆盖度**
- [ ] 线性排列元素群运行了轴线扫描？
- [ ] 居中/对齐元素做了三层对齐检查？
- [ ] 列出了"已确认无问题"清单？

**C 组：截图正确性（🆕）**
- [ ] 每张对比图的左边来自设计稿，右边来自开发稿？
- [ ] 两张图明显不同（不是同一来源）？
- [ ] 红框只画在开发稿上，没画在设计稿上？

**D 组：终审（🆕）**
- [ ] 终审自检全部通过？
- [ ] 终审报告附在最终输出中？

---

## C端外卖场景重点关注
- **轴线扫描必检**：路线节点、底部 Tab、商品列表、Banner 指示器
- **多层对齐必检**：卡片标题、Header 文字、价格标签、徽章/角标
- 商品卡 / 店铺卡：图片比例、标题字号、价格颜色
- 操作按钮：颜色、宽高比、左右顺序

---

## 失败案例库

### 案例1（2026-05-21）：节点偏移漏检
**现象**：路线图中间「送」节点向左偏移 10px，未检出。
**根因**：组件整体看，没逐节点比对。
**修复**：增加轴线扫描机制。

### 案例2（2026-05-21）：多层对齐漏检
**现象**：「顺路度 96」在卡片内偏右，未检出。
**根因**：只查"文字 vs 直接父容器"一层，漏了"容器 vs 卡片"层。
**修复**：增加三层对齐检查。

### 案例3（2026-05-21）：自我比对错误
**现象**：可能出现"对比图两边都来自同一张图"的低级错误，看起来一样无法发现问题。
**根因**：裁切代码混淆了 design 和 dev 来源。
**修复**：增加铁律8（截图来源约束）+ Step 5.2 数据校验断言。

### 案例4（2026-05-21）：交付前缺少终审
**现象**：报告生成完直接交付，存在的截图错位、内容错误没被发现。
**根因**：缺少"生成后再过一遍"的终审环节。
**修复**：增加 Step 6 终审自检，作为最后一道强制关卡。
